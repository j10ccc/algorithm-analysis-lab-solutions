{
  "name": "algorithm-assignments-monorepo",
  "descriptions": "bth004 assignments implements",
  "private": true,
  "scripts": {
    "greedy": "bun index.ts --algorithm greedy",
    "neighbor": "bun index.ts --algorithm neighborSearch",
    "tabu": "bun index.ts --algorithm tabuSearch",
    "bubble": "bun index.ts --algorithm bubble",
    "merge": "bun index.ts --algorithm merge"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "chalk": "^5.3.0",
    "assignment1-solutions": "workspace:*"
  },
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}